# NetWatch dashboard — Python dependencies
fastapi==0.138.2              # held as a set with starlette/pydantic (see note below)
pydantic==2.13.4             # app uses the pydantic v1 API — do NOT bump to v2
starlette==1.3.1             # FastAPI 0.101 requires <0.28 — bump together with fastapi
uvicorn[standard]==0.49.0
jinja2==3.1.6                 # security update
python-multipart==0.0.32      # form-based login; fixes CVE-2024-53981 (DoS)
requests==2.34.2              # security update (.netrc leak / cert handling)
psutil==7.2.2                 # host metrics
maxminddb==3.1.1              # MaxMind GeoLite2 country/ASN lookups
itsdangerous==2.2.0           # signed session cookie
urllib3==2.7.0                # security update (redirect header leak)

# NOTE: fastapi + starlette + pydantic are held together — the app uses the
# pydantic v1 API. Newer FastAPI still supports pydantic v1, so this trio can be
# upgraded; it is left to a reviewed Dependabot PR so it moves (and is tested) as one.

# Front-end libs (Chart.js, D3, topojson, world-atlas) load from a CDN in
# templates/index.html — no Python dependency. ntopng/Redis/Suricata/EveBox
# are external services, not pip packages.
